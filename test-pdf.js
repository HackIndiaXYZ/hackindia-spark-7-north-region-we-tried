const fs = require('fs');
const pdfParse = require('pdf-parse');
async function test() {
  // Let's create a dummy PDF string and see if it throws? No, we don't have a test PDF.
  console.log("pdf-parse loaded");
}
test();
